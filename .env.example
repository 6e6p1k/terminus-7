# Neon Postgres
DATABASE_URL="postgres://user:password@host:port/db-name"

# Vercel AI Gateway auth (either works)
# Preferred on Vercel: OIDC via `vercel env pull` → VERCEL_OIDC_TOKEN
# Static key alternative:
AI_GATEWAY_API_KEY=""
